# 회차별 기록

평가셋 12건 (면세3 · 검역3 · 멸종위기종2 · 면세점2 · 범위밖2) · 모델 `gpt-4.1-mini` · 채점기 동일 모델
지표: **도구** = 도구 호출 적절성, **답변** = 답변 적절성

측정 규칙 (`project_2/chatbot/PLAYBOOK.md` 5번) — 한 번에 한 군데만 / 최소 2회 / 2회 연속 통과해야 "수정됨" / 1건 차이는 변화 없음 / 원복도 기록

---

## #-1 자(尺) 교정 — 측정 전에 채점기와 가드레일을 고쳤다

기준선을 재려고 처음 돌렸을 때 **도구 83.3% / 답변 25.0%** 가 나왔다. 그런데 실패 목록이 이상했다.

```
금지 어김 ['모든 식품류는 예외 없이 검역 대상이라고 답하지 않았다']
근거없는 숫자 ['09', '17', '2026']
```

두 가지가 망가져 있었다.

| 무엇이 | 어떻게 | 고친 것 |
|---|---|---|
| 채점기 | `list[str]` 로 "담은 사실"을 받으니 판단 근거 문장을 목록에 넣었다. 어긴 것이 없으면 `["없음"]` 을 돌려줘 위반 1건으로 세어졌고, **안 어긴 항목도 "~라고 답하지 않았다"** 로 어김 목록에 들어갔다 | 항목을 번호로 주고 `list[bool]` 로만 받는다. 길이가 다르면 맞춰 자른다 |
| 가드레일 | 답변 끝에 `(2026-09-17 기준)` 을 붙이라고 **내가 지시했는데**, 검증이 그 `09`·`17` 을 근거에 없는 숫자로 잡아 재작성까지 시켰다 | 기준일을 근거에 있는 값으로 센다 (`agent.verify`) |

이건 개선 회차가 아니다. **자가 틀린 상태의 25.0% 는 기준선이 아니다.** 고친 자로 다시 쟀다.

> 채점기 자체 검증(`--self-check`)을 먼저 만들어 둔 것이 여기서 값을 했다.
> 다만 자체 검증은 3건뿐이어서 `["없음"]` 만 잡고 "~라고 답하지 않았다" 는 놓쳤다 — 평가셋 전체를 돌려야 드러났다.

---

## #0 기준선

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| base-1 | **10/12 = 83.3%** | **7/12 = 58.3%** | D1 D2 D3 C1 X1 |
| base-2 | **10/12 = 83.3%** | **7/12 = 58.3%** | D1 D2 D3 C1 X1 |

두 번이 **완전히 같았다** (`temperature=0`). 지난 과제에서 같은 코드로 ±3건이 흔들렸던 것과 달라,
이 평가셋에서는 1건 차이도 신호로 읽을 수 있다.

실패 원인 분류:

| 건 | 카테고리 | 원인 | 분류 |
|---|---|---|---|
| D1 | 면세 | 사례집의 **해외직구(발송) 기준** 「150불 이하 1병(1L)」을 인용했다. 여행자 휴대품 기준 「2L·400달러」는 `duty.md` 에 있는데 검색이 안 가져왔다 | 근거 선택 오류 |
| D2 | 면세 | 800달러를 빠뜨렸다 (근거에는 있다) | 사실 누락 |
| D3 | 면세 | 「2년 내 2회 이상 60%」를 빠뜨렸다 | 사실 누락 |
| C1 | 멸종위기종 | **`cites.md` 에 품목 예시가 아예 없다** — 악어·지갑·가죽·핸드백·가공품 전부 0건. 검색 점수 0 → 근거 없음 → 넘김 | **문서 부족** |
| X1 | 범위밖 | 「한국에서 산 홍삼을 미국 갈 때」를 `검역` 으로 분류해 도구를 불렀다 | 분류 오류 |

D1 과 C1 을 처음부터 예상했고 그대로 나왔다. C1 은 검색 버그가 아니라 **문서가 없어서** 못 찾은 것이다 —
말로도 코드로도 안 풀리는 종류다.

---

## #1 멸종위기종 문서 보강

- **고친 곳**: `sources.yaml` 에 `cites_items.md` 추가 (인천본부세관 CITES 안내 — 부분품/가공품 목록과 부속서 I·II·III 표)
- **왜**: `cites.md` 는 협약 개요·허가서·처벌만 담고 있어 "악어가죽 지갑" 같은 실제 품목 문의에 걸릴 말이 한 개도 없었다.
  과제 요구사항이 *"연결할 부분이 없는 카테고리가 있다면 문서를 보강한다"* 고 한 바로 그 경우다.
- **손대지 않은 것**: 프롬프트, 검색 함수, 분류 지침. 한 번에 한 군데만.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp1-1 | **11/12 = 91.7%** | **8/12 = 66.7%** | D1 D2 D3 X1 |
| imp1-2 | **11/12 = 91.7%** | **8/12 = 66.7%** | D1 D2 D3 X1 |

**판정: 수정됨.** C1 이 두 회차 연속 통과했고 실패 목록에서 사라졌다.
도구 83.3% → 91.7%, 답변 58.3% → 66.7%. 다른 건에 부작용 없음 (실패 목록이 정확히 C1 하나만 줄었다).

검색 함수도 프롬프트도 건드리지 않았다. **문서가 없어서 못 찾던 것을 문서로 고쳤다.**
`cites.md` 에는 "악어·지갑·가죽·핸드백·가공품" 이 0건이었고, 보강한 `cites_items.md` 에
부분품(뿔·이·털·가죽)과 가공품(박제·핸드백·지갑·벨트) 목록이 들어 있다.

---

## #2 검색에 IDF — 흔한 말의 가중치를 낮춘다

- **고친 곳**: `context.search` — 점수를 단어 빈도의 생 합계에서 `빈도 × IDF` 로
- **왜**: 통관 문서에서 `신고` 는 거의 모든 절에 나와 **절을 구분하지 못한다.** 그런데 점수를 지배했다.
  정작 「가산세 40%·2년내 2회 이상 60%」 절은 7위(8점)로 밀려 답변이 *"자료에 없습니다"* 가 됐다.
- 처음엔 절 길이 보정을 의심했으나 길이 분포가 343~770자로 고르게 나와, 길이가 아니라 **어휘**가 문제였다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp2-1 | 11/12 = 91.7% | **9/12 = 75.0%** | D1 D3 X1 |
| imp2-2 | 11/12 = 91.7% | **9/12 = 75.0%** | D1 D3 X1 |

**판정: 수정됨 (D2).** 66.7% → 75.0%. D1·D3 는 그대로 — IDF 만으로는 부족했다.

---

## #3 규정 문서 우선, 사례집은 보조

- **고친 곳**: `sources.yaml` 에 `보조: true` (casebook), `context.search` 가 규정 문서를 먼저 채운다
- **왜**: 면세 카테고리에 **사례집 10절 vs 규정 3절.** 점수와 무관하게 수로 밀린다.
  사례집은 보충 설명이지 1차 근거가 아니다.

**처음 시도에서 버그를 넣었다.** `main[: top - 1]` 로 무조건 한 자리를 비워 두었더니,
보조 문서가 **없는** 카테고리(검역)의 근거가 3절 → 2절로 줄어 **Q2 가 새로 깨졌다.**

| 회차 | 도구 | 답변 | 실패 건 | |
|---|---|---|---|---|
| imp3-1 | 91.7% | 83.3% | Q2 X1 | 버그 있음 |
| imp3-2 | 91.7% | 75.0% | D3 Q2 X1 | 버그 있음 — Q2 회귀 |
| imp3b-1 | 91.7% | **83.3%** | D3 X1 | 버그 수정 |
| imp3b-2 | 91.7% | **83.3%** | D3 X1 | 버그 수정 |

**판정: 수정됨 (D1).** 75.0% → 83.3%. 150불/1병 함정이 근거에서 사라졌고 Q2 회귀도 없다.

> 회귀를 잡은 것은 점수가 아니라 **실패 목록**이었다. imp3-1 의 83.3% 만 봤다면
> "좋아졌다"고 넘어갔을 텐데, 목록에 없던 Q2 가 새로 들어온 것이 보였다.

---

## #4 분류에 "방향" 규칙 한 줄

- **고친 곳**: `prompts.CLASSIFY` — *"한국에서 **나가는** 이야기는 물건이 무엇이든 범위밖"*
- **왜**: 「홍삼을 미국 갈 때」를 품목만 보고 `검역` 으로 분류했다. 방향이 먼저다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp4-1 | **12/12 = 100%** | 12/12 = 100% | 없음 |
| imp4-2 | **12/12 = 100%** | 11/12 = 91.7% | D3 |

**판정: 수정됨 (X1).** 도구 91.7% → **100%**, 2회 연속. D3 는 왔다갔다 → 미확정.

---

## #5 나란히 놓인 두 경우를 둘 다 쓴다

- **고친 곳**: `prompts.ANSWER` 한 줄 — *"[근거]에 나란히 놓인 두 경우(자진신고 시 / 미신고 시)는 둘 다 쓴다.
  질문이 한쪽만 물어도 다른 쪽을 같이 알려 주는 것이 상담이다"*
- **왜**: D3 가 「미신고 40%·60%」는 쓰면서 바로 옆의 「자진신고 30% 경감」을 빠뜨렸다.
  질문이 "안 하고 걸리면"이라 한쪽만 답한 것인데, 상담으로는 둘 다 알려 주는 게 맞다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp5-1 | **100%** | **100%** | 없음 |
| imp5-2 | **100%** | **100%** | 없음 |
| imp5-3 | **100%** | **100%** | 없음 |

**판정: 수정됨 (D3).** 3회 연속 만점.

---

## #6 동의어 — 사람 말과 문서 말을 잇는다  ⚠️ 회귀

**시연에서 나왔다.** 「예전엔 술 2병까지 됐잖아요. 지금은 어떻게 되나요?」 가 **넘김** 처리됐다.
평가셋 12건이 100% 이던 시점이다 — 12건이 못 잡는 구멍이 있었다는 뜻이다.

원인은 두 겹이었다.

1. `술` 은 **한 글자라 토큰에서 버려진다** (`len < 2`). `잣`·`꿀`·`옷` 도 마찬가지
2. 살려도 소용없다. **규정 문서는 `주류` 라 쓰고 사람은 `술` 이라 쓴다.** 어휘가 안 이어진다

- **고친 곳**: `context.expand()` — 질문에 문서의 말을 덧붙인다 (`술` → `주류`, `소시지` → `육가공품`).
  원래 말은 지우지 않는다. 단어 **앞머리**로만 맞춰 "미술품"이 "주류"로 새지 않게 했다
- 오른쪽 말은 반드시 `docs/` 에 실제로 있는 것만 넣었다. 없는 말은 아무 효과가 없다

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp6-1 | 100% | **11/12 = 91.7%** | D1 |
| imp6-2 | 100% | **11/12 = 91.7%** | D1 |

**판정: 회귀.** 시연 문의는 고쳤지만 **D1 이 도로 깨졌다** (100% → 91.7%).
`술` → `주류` 로 확장하니 사례집의 「술, 담배의 면세기준」 절이 점수를 올려 근거에 들어왔고,
모델이 거기 적힌 **해외직구 기준(150달러·1병)** 을 인용했다.

**원복하지 않았다.** #3 에서 규정 문서를 앞에 세워 이 함정을 *피했을 뿐 고친 게 아니라는 것*이
드러났기 때문이다. 근거에 들어오기만 하면 모델은 여전히 그걸 고른다. 근본을 #7 에서 고쳤다.

---

## #7 다른 제도를 다루는 쪽을 근거에서 뺀다

- **고친 곳**: `sources.yaml` 에 `쪽제외: ["해외에서 발송되는", "발송되는 술"]`, `fetch_docs.fetch_pdf` 가 그 쪽을 건너뛴다
- **왜**: 사례집 24쪽은 「우리나라 거주자가 해외에서 **발송되는** 술, 담배를 반입하는 경우」 —
  **해외직구** 기준이다. 이 봇은 여행자가 **직접 들고 오는** 경우만 다룬다.
  같은 "술 면세" 제목 아래 숫자가 완전히 다르고(150달러·1병 vs 2L·400달러), 근거에 있으면 모델이 인용한다.
- 지우는 것이 아니라 **범위를 맞추는 것**이다. 우리가 답하지 않는 제도의 문서는 애초에 근거가 아니다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp7-1 | **100%** | **100%** | 없음 |
| imp7-2 | **100%** | **100%** | 없음 |
| imp7-3 | **100%** | **100%** | 없음 |

**판정: 수정됨.** 3회 연속 만점. 시연 문의도 바르게 답한다:

```
문의   예전엔 술 2병까지 됐잖아요. 지금은 어떻게 되나요?
답변   현재 주류 면세범위는 전체 용량이 2L 이하이고 총 가격이 미화 400달러 이하입니다.
       만 19세 미만(출생년도 기준) 미성년자는 주류 면세범위가 없습니다. (2026-09-17 기준)
```

과거 전제("예전엔 2병")를 **반박하지도, 따라가지도 않고** 현재 기준만 답한다.

---

## 분류가 애매한 질문으로 찔러 보기 (`probe_ambiguous.py`)

평가셋 12건은 카테고리가 깔끔하게 갈리는 문항만 있다. 실제 문의는 두 영역에 걸친다.
경계에 일부러 걸치는 8건을 만들어 돌렸다. **채점하지 않는다** — 어디로 가는지 보는 것이 목적이다.

**결과: 8건 중 3건 실패.** 12건이 100% 이던 시점이다.

| | 질문 | 걸치는 지점 | 결과 |
|---|---|---|---|
| 3 | 독일 소시지 30달러어치 샀는데 면세 한도 안이면 괜찮죠? | 면세 ↔ 검역 | ❌ **"신고 없이 통과할 수 있다"** |
| 4 | 캐비어를 선물로 받았는데 가져올 수 있나요? | 검역 ↔ 멸종위기종 | ❌ CITES 를 못 봄 |
| 6 | 해외 쇼핑몰에서 택배로 받는 술은 몇 병까지? | 면세 ↔ 범위밖 | ❌ 휴대품 기준을 적용 |
| 2 | 입국장면세점에서 산 위스키도 술 면세 한도에? | 면세점 ↔ 면세 | ⚠️ 공제 순서를 흐리게 설명 |
| 1·5·7·8 | 악어가죽+800달러 / 사향 화장품 / 반려견 / 폐기 | | ✅ |

3번이 위험했다. **검역이 막는 물건에 "신고 없이 통과"** 라고 답했다. 믿고 신고 안 하면 과태료다.

---

## #8 경로 규칙 — 부쳐서 받는 것은 범위밖

- **고친 곳**: `prompts.CLASSIFY` 한 줄 — *"택배·배송대행·해외직구처럼 물건만 부쳐서 받는 경우는
  면세 기준이 아예 다른 제도이므로 범위밖"*
- **왜**: #7 에서 직구 문서를 근거에서 뺐더니 **반대 방향 오류**가 생겼다 — 직구 질문에 휴대품 기준을 답한다.
  문서를 뺐으면 질문도 받지 말아야 한다.

| 회차 | 도구 | 답변 |
|---|---|---|
| imp8-1 | 100% | 100% |
| imp8-2 | 100% | 100% |

**판정: 수정됨 (6번).** 12건 회귀 없음.

---

## #9 금지·제한 품목 사전 확인 — 도구를 둘 부른다

- **고친 곳**: `context.RESTRICTED` + `assemble()` — 문의에 **물건 자체가 막히는 품목**(소시지·치즈·생과일 /
  악어·상아·산호…)이 있으면 분류가 무엇이든 그 영역의 도구를 **같이** 부른다
- **왜**: 분류는 *"무엇을 묻는가"* 를 맞혔다. 3번은 금액을 물었으니 `면세` 가 맞다.
  그런데 답에 필요한 것은 *"무엇을 가져오는가"* 였다. 한 문의에 도구 하나라는 제약이 문제였지
  분류가 틀린 게 아니다 — **프롬프트로는 고칠 수 없는 종류다.**
- 이미 그 영역을 보고 있으면 두 번 부르지 않고, **범위밖은 품목이 무엇이든 도구를 부르지 않는다**

| 회차 | 도구 | 답변 |
|---|---|---|
| imp9-1 | 100% | 100% |
| imp9-2 | 100% | 100% |

**판정: 수정됨 (3번).** 12건 회귀 없음.

```
독일 소시지 30달러어치 샀는데 면세 한도 안이면 괜찮죠?
  도구 ['lookup_duty', 'lookup_quarantine']
  → 800달러 이하에 해당하지만, 육류·육가공품은 수입 가능국가에서 수출국 검역증명서를
    휴대한 경우에만 검사 후 반입 가능합니다. 검역증명서가 없으면 반입이 제한될 수 있습니다.
```

**4번(캐비어)은 남았다.** 품목 사전에는 있지만 `docs/` 의 CITES 문서에 「철갑상어」가 없어
검색이 빈 결과를 돌려주고, 그래서 도구가 붙지 않는다. **문서 부족**이지 배선 문제가 아니다
(#1 의 악어가죽과 같은 종류). 봇은 "자료에 없다"고 정직하게 답하므로 위험하지는 않다.

---

## #10 CITES 종 목록 절을 받아오고, 용어 연결을 명시한다

**한 회차에 두 곳을 고쳤다.** 규칙을 어긴 것이 아니라, **반쪽만으로는 아무 효과가 없음이 증명되는**
짝이었다 — 목록을 받아와도 `캐비어` 가 목록의 `철갑상어` 와 안 이어지고, 연결만 만들어도 목록이 없다.

### ① 목록 문서 — 필요한 절만 떼어 온다

`철갑상어` 가 실려 있는 페이지는 **관세청 여행자 통관 안내 하나뿐이었다.** (다른 세관 페이지 4곳,
국립생물자원관·국립생태원·환경부까지 찔러 봤지만 목록이 짧거나 자바스크립트로 그려진다.)
그런데 그 페이지는 이미 `duty.md` 로 **면세** 에 쓰고 있다.

- **고친 곳**: `fetch_docs.fetch` 에 `절키워드` 추가 — 페이지를 통째로 복사하지 않고 **절 단위로** 가져온다
- `sources.yaml` 에 `cites_species.md` (같은 URL, `절키워드: ["철갑상어"]`, 카테고리 `멸종위기종`)
- 처음엔 `["CITES"]` 로 걸었더니 **가산세 절까지 딸려왔다** (그 절도 CITES 를 언급한다).
  품목 목록 절에만 있는 말로 좁혔다

### ② 용어 연결을 답변 단계에 넘긴다

목록을 넣고 도구도 둘 다 불렀는데 봇은 여전히 *"자료에 없다"* 고 답했다.

근거에 `철갑상어` 가 있어도 **«캐비어 = 철갑상어 알» 은 문서에 없는 연결**이라 모델이 스스로 잇지 않았다.
**옳은 태도다** — 그렇게 이어 주기 시작하면 근거 없는 추론이 열린다.

- **고친 곳**: `context.aliases()` + `prompts.ANSWER` 에 `[용어 연결]` 칸 —
  이 문의에 실제로 적용된 «사람 말 → 문서 말» 짝만 넘긴다
- *"이 표에 있는 것만 같은 것으로 본다"* 고 못 박았다. 모델이 연결을 늘리지 못한다
- **데모 화면에도 그대로 보인다** (③ 용어 연결). 우리가 만든 연결임을 보는 사람이 판단할 수 있게

| 회차 | 도구 | 답변 |
|---|---|---|
| imp10-1 | 100% | 100% |
| imp10-2 | 100% | 100% |

**판정: 수정됨 (4번).** 12건 회귀 없음.

```
캐비어를 선물로 받았는데 가져올 수 있나요?
  분류 검역 · 도구 ['lookup_quarantine', 'lookup_cites'] · 용어연결 캐비어 → 철갑상어
  → 캐비어(철갑상어)는 CITES 에서 보호하는 품목입니다. 살아있는 동물뿐 아니라
    이들을 사용하여 만든 제품·가공품도 포함됩니다. (…) 출국 전 재확인하시기 바랍니다.
```

---

## 경계 질문 8건 — 처음과 지금

| | 질문 | 처음 | 지금 |
|---|---|---|---|
| 1 | 악어가죽 지갑도 800달러 안에 들어가면? | ✅ | ✅ |
| 2 | 입국장면세점에서 산 위스키도 술 면세 한도에? | ⚠️ 흐림 | ⚠️ 흐림 |
| 3 | 독일 소시지 30달러어치 면세 한도 안이면? | ❌ "신고 없이 통과" | ✅ 도구 2개 |
| 4 | 캐비어를 선물로 받았는데? | ❌ | ✅ 도구 2개 |
| 5 | 화장품에 사향 성분이? | ✅ | ✅ |
| 6 | 택배로 받는 술은 몇 병까지? | ❌ | ✅ 범위밖 |
| 7 | 반려견을 데리고 입국하려면? | ✅ | ✅ |
| 8 | 한도 넘은 걸 공항에서 버리고 나와도? | ✅ | ✅ |

**3건 실패 → 0건 실패** (2번은 답변이 흐린 것이라 실패로 세지 않았다).

---

## 전체 흐름

| 회차 | 고친 곳 | 성격 | 도구 | 답변 |
|---|---|---|---|---|
| #0 기준선 | — | | 83.3% | 58.3% |
| #1 | 멸종위기종 문서 보강 | 문서 | 91.7% | 66.7% |
| #2 | 검색에 IDF | 검색 | 91.7% | 75.0% |
| #3 | 규정 문서 우선 (+ 회귀 1건 수정) | 검색 | 91.7% | 83.3% |
| #4 | 분류에 방향 규칙 | 프롬프트 | **100%** | 91.7% |
| #5 | 나란한 두 경우 둘 다 | 프롬프트 | **100%** | **100%** |
| #6 | 동의어 확장 | 검색 | 100% | 91.7% ⚠️ 회귀 |
| #7 | 다른 제도 쪽 제외 | 문서 | **100%** | **100%** |
| #8 | 경로 규칙 (택배는 범위밖) | 프롬프트 | **100%** | **100%** |
| #9 | 금지·제한 품목 사전 확인 | 구조 | **100%** | **100%** |
| #10 | CITES 종 목록 절 + 용어 연결 명시 | 문서 + 프롬프트 | **100%** | **100%** |

**손댄 곳의 성격**: 문서 4 · 검색 3 · 프롬프트 4 · 구조 1.
케이스별 코드 관문은 **한 개도 넣지 않았다.**

## 12건 100% 가 말해 주지 않는 것

| 무엇이 찾아냈나 | 무엇을 | 회차 |
|---|---|---|
| 평가셋 12건 | 근거 선택, 사실 누락, 분류 방향 | #1~#5 |
| **시연 (사람이 직접)** | 한 글자 품목, 사람 말 ↔ 문서 말 | #6·#7 |
| **경계 질문 8건** | 한 문의 두 영역, 경로 구분, 문서에 없는 연결 | #8~#10 |

세 번 다 **12건이 만점인 상태에서** 나왔다. 100% 는 천장이 아니라 **평가셋의 크기**다.

## 다음에 할 일

1. **검증용 평가셋** — 시연·경계 질문에서 나온 것을 정답과 함께 `data/holdout.json` 으로.
   지금 12건은 개선용이 되어 버렸다
2. **2번(입국장면세점 공제 순서)** — 답변이 흐리다. 공제 순서를 절로 따로 떼어 두는 편이 나을 수 있다
3. **동의어·품목 사전을 문서에서 뽑기** — 지금 `SYNONYMS`(27) 과 `RESTRICTED`(45) 는 손으로 적었다.
   문서 어휘와 자동으로 맞추는 편이 낫다
4. **검역 문서 자동 갱신** — 주 1회 `fetch_docs.py 검역`

## 손대지 않기로 한 것

- `context.MAX = 600` (절 길이) — 지금 절 분할이 근거 표시 단위로 알맞다
- `verify` 를 숫자 외로 넓히지 않기 — 품목명까지 역추적하면 오탐이 폭발한다
- 평가셋을 프롬프트에 넣지 않기 (`data/fewshot.json` 만 예시로 쓴다)
- **`RESTRICTED`·`SYNONYMS` 를 문항 단위로 늘리지 않기** — 부류가 아니라 케이스가 되면 그게 코드 관문이다
- **용어 연결을 모델이 늘리게 하지 않기** — 사전에 적힌 짝만 같은 것으로 본다
