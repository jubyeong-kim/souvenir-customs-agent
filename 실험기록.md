# 회차별 기록

평가셋 12건 (면세3 · 검역3 · 멸종위기종2 · 면세점2 · 범위밖2) · 모델 `gpt-4.1-mini` · 채점기 동일 모델
지표: **도구** = 도구 호출 적절성, **답변** = 답변 적절성

측정 규칙 (`project_2/chatbot/PLAYBOOK.md` 5번) — 한 번에 한 군데만 / 최소 2회 / 2회 연속 통과해야 "수정됨" / 1건 차이는 변화 없음 / 원복도 기록

---

## #-1 자(尺) 교정 — 측정 전에 채점기와 가드레일을 고쳤다

기준선을 재려고 처음 돌렸을 때 **도구 83.3% / 답변 25.0%** 가 나왔다. 그런데 실패 목록이 이상했다.

```
금지 어김 ['모든 식품류는 예외 없이 검역 대상이라고 답하지 않았다']
근거없는 숫자 ['09', '17', '2026']
```

두 가지가 망가져 있었다.

| 무엇이 | 어떻게 | 고친 것 |
|---|---|---|
| 채점기 | `list[str]` 로 "담은 사실"을 받으니 판단 근거 문장을 목록에 넣었다. 어긴 것이 없으면 `["없음"]` 을 돌려줘 위반 1건으로 세어졌고, **안 어긴 항목도 "~라고 답하지 않았다"** 로 어김 목록에 들어갔다 | 항목을 번호로 주고 `list[bool]` 로만 받는다. 길이가 다르면 맞춰 자른다 |
| 가드레일 | 답변 끝에 `(2026-09-17 기준)` 을 붙이라고 **내가 지시했는데**, 검증이 그 `09`·`17` 을 근거에 없는 숫자로 잡아 재작성까지 시켰다 | 기준일을 근거에 있는 값으로 센다 (`agent.verify`) |

이건 개선 회차가 아니다. **자가 틀린 상태의 25.0% 는 기준선이 아니다.** 고친 자로 다시 쟀다.

> 채점기 자체 검증(`--self-check`)을 먼저 만들어 둔 것이 여기서 값을 했다.
> 다만 자체 검증은 3건뿐이어서 `["없음"]` 만 잡고 "~라고 답하지 않았다" 는 놓쳤다 — 평가셋 전체를 돌려야 드러났다.

---

## #0 기준선

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| base-1 | **10/12 = 83.3%** | **7/12 = 58.3%** | D1 D2 D3 C1 X1 |
| base-2 | **10/12 = 83.3%** | **7/12 = 58.3%** | D1 D2 D3 C1 X1 |

두 번이 **완전히 같았다** (`temperature=0`). 지난 과제에서 같은 코드로 ±3건이 흔들렸던 것과 달라,
이 평가셋에서는 1건 차이도 신호로 읽을 수 있다.

실패 원인 분류:

| 건 | 카테고리 | 원인 | 분류 |
|---|---|---|---|
| D1 | 면세 | 사례집의 **해외직구(발송) 기준** 「150불 이하 1병(1L)」을 인용했다. 여행자 휴대품 기준 「2L·400달러」는 `duty.md` 에 있는데 검색이 안 가져왔다 | 근거 선택 오류 |
| D2 | 면세 | 800달러를 빠뜨렸다 (근거에는 있다) | 사실 누락 |
| D3 | 면세 | 「2년 내 2회 이상 60%」를 빠뜨렸다 | 사실 누락 |
| C1 | 멸종위기종 | **`cites.md` 에 품목 예시가 아예 없다** — 악어·지갑·가죽·핸드백·가공품 전부 0건. 검색 점수 0 → 근거 없음 → 넘김 | **문서 부족** |
| X1 | 범위밖 | 「한국에서 산 홍삼을 미국 갈 때」를 `검역` 으로 분류해 도구를 불렀다 | 분류 오류 |

D1 과 C1 을 처음부터 예상했고 그대로 나왔다. C1 은 검색 버그가 아니라 **문서가 없어서** 못 찾은 것이다 —
말로도 코드로도 안 풀리는 종류다.

---

## #1 멸종위기종 문서 보강

- **고친 곳**: `sources.yaml` 에 `cites_items.md` 추가 (인천본부세관 CITES 안내 — 부분품/가공품 목록과 부속서 I·II·III 표)
- **왜**: `cites.md` 는 협약 개요·허가서·처벌만 담고 있어 "악어가죽 지갑" 같은 실제 품목 문의에 걸릴 말이 한 개도 없었다.
  과제 요구사항이 *"연결할 부분이 없는 카테고리가 있다면 문서를 보강한다"* 고 한 바로 그 경우다.
- **손대지 않은 것**: 프롬프트, 검색 함수, 분류 지침. 한 번에 한 군데만.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp1-1 | **11/12 = 91.7%** | **8/12 = 66.7%** | D1 D2 D3 X1 |
| imp1-2 | **11/12 = 91.7%** | **8/12 = 66.7%** | D1 D2 D3 X1 |

**판정: 수정됨.** C1 이 두 회차 연속 통과했고 실패 목록에서 사라졌다.
도구 83.3% → 91.7%, 답변 58.3% → 66.7%. 다른 건에 부작용 없음 (실패 목록이 정확히 C1 하나만 줄었다).

검색 함수도 프롬프트도 건드리지 않았다. **문서가 없어서 못 찾던 것을 문서로 고쳤다.**
`cites.md` 에는 "악어·지갑·가죽·핸드백·가공품" 이 0건이었고, 보강한 `cites_items.md` 에
부분품(뿔·이·털·가죽)과 가공품(박제·핸드백·지갑·벨트) 목록이 들어 있다.

---

## #2 검색에 IDF — 흔한 말의 가중치를 낮춘다

- **고친 곳**: `context.search` — 점수를 단어 빈도의 생 합계에서 `빈도 × IDF` 로
- **왜**: 통관 문서에서 `신고` 는 거의 모든 절에 나와 **절을 구분하지 못한다.** 그런데 점수를 지배했다.
  정작 「가산세 40%·2년내 2회 이상 60%」 절은 7위(8점)로 밀려 답변이 *"자료에 없습니다"* 가 됐다.
- 처음엔 절 길이 보정을 의심했으나 길이 분포가 343~770자로 고르게 나와, 길이가 아니라 **어휘**가 문제였다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp2-1 | 11/12 = 91.7% | **9/12 = 75.0%** | D1 D3 X1 |
| imp2-2 | 11/12 = 91.7% | **9/12 = 75.0%** | D1 D3 X1 |

**판정: 수정됨 (D2).** 66.7% → 75.0%. D1·D3 는 그대로 — IDF 만으로는 부족했다.

---

## #3 규정 문서 우선, 사례집은 보조

- **고친 곳**: `sources.yaml` 에 `보조: true` (casebook), `context.search` 가 규정 문서를 먼저 채운다
- **왜**: 면세 카테고리에 **사례집 10절 vs 규정 3절.** 점수와 무관하게 수로 밀린다.
  사례집은 보충 설명이지 1차 근거가 아니다.

**처음 시도에서 버그를 넣었다.** `main[: top - 1]` 로 무조건 한 자리를 비워 두었더니,
보조 문서가 **없는** 카테고리(검역)의 근거가 3절 → 2절로 줄어 **Q2 가 새로 깨졌다.**

| 회차 | 도구 | 답변 | 실패 건 | |
|---|---|---|---|---|
| imp3-1 | 91.7% | 83.3% | Q2 X1 | 버그 있음 |
| imp3-2 | 91.7% | 75.0% | D3 Q2 X1 | 버그 있음 — Q2 회귀 |
| imp3b-1 | 91.7% | **83.3%** | D3 X1 | 버그 수정 |
| imp3b-2 | 91.7% | **83.3%** | D3 X1 | 버그 수정 |

**판정: 수정됨 (D1).** 75.0% → 83.3%. 150불/1병 함정이 근거에서 사라졌고 Q2 회귀도 없다.

> 회귀를 잡은 것은 점수가 아니라 **실패 목록**이었다. imp3-1 의 83.3% 만 봤다면
> "좋아졌다"고 넘어갔을 텐데, 목록에 없던 Q2 가 새로 들어온 것이 보였다.

---

## #4 분류에 "방향" 규칙 한 줄

- **고친 곳**: `prompts.CLASSIFY` — *"한국에서 **나가는** 이야기는 물건이 무엇이든 범위밖"*
- **왜**: 「홍삼을 미국 갈 때」를 품목만 보고 `검역` 으로 분류했다. 방향이 먼저다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp4-1 | **12/12 = 100%** | 12/12 = 100% | 없음 |
| imp4-2 | **12/12 = 100%** | 11/12 = 91.7% | D3 |

**판정: 수정됨 (X1).** 도구 91.7% → **100%**, 2회 연속. D3 는 왔다갔다 → 미확정.

---

## #5 나란히 놓인 두 경우를 둘 다 쓴다

- **고친 곳**: `prompts.ANSWER` 한 줄 — *"[근거]에 나란히 놓인 두 경우(자진신고 시 / 미신고 시)는 둘 다 쓴다.
  질문이 한쪽만 물어도 다른 쪽을 같이 알려 주는 것이 상담이다"*
- **왜**: D3 가 「미신고 40%·60%」는 쓰면서 바로 옆의 「자진신고 30% 경감」을 빠뜨렸다.
  질문이 "안 하고 걸리면"이라 한쪽만 답한 것인데, 상담으로는 둘 다 알려 주는 게 맞다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp5-1 | **100%** | **100%** | 없음 |
| imp5-2 | **100%** | **100%** | 없음 |
| imp5-3 | **100%** | **100%** | 없음 |

**판정: 수정됨 (D3).** 3회 연속 만점.

---

## #6 동의어 — 사람 말과 문서 말을 잇는다  ⚠️ 회귀

**시연에서 나왔다.** 「예전엔 술 2병까지 됐잖아요. 지금은 어떻게 되나요?」 가 **넘김** 처리됐다.
평가셋 12건이 100% 이던 시점이다 — 12건이 못 잡는 구멍이 있었다는 뜻이다.

원인은 두 겹이었다.

1. `술` 은 **한 글자라 토큰에서 버려진다** (`len < 2`). `잣`·`꿀`·`옷` 도 마찬가지
2. 살려도 소용없다. **규정 문서는 `주류` 라 쓰고 사람은 `술` 이라 쓴다.** 어휘가 안 이어진다

- **고친 곳**: `context.expand()` — 질문에 문서의 말을 덧붙인다 (`술` → `주류`, `소시지` → `육가공품`).
  원래 말은 지우지 않는다. 단어 **앞머리**로만 맞춰 "미술품"이 "주류"로 새지 않게 했다
- 오른쪽 말은 반드시 `docs/` 에 실제로 있는 것만 넣었다. 없는 말은 아무 효과가 없다

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp6-1 | 100% | **11/12 = 91.7%** | D1 |
| imp6-2 | 100% | **11/12 = 91.7%** | D1 |

**판정: 회귀.** 시연 문의는 고쳤지만 **D1 이 도로 깨졌다** (100% → 91.7%).
`술` → `주류` 로 확장하니 사례집의 「술, 담배의 면세기준」 절이 점수를 올려 근거에 들어왔고,
모델이 거기 적힌 **해외직구 기준(150달러·1병)** 을 인용했다.

**원복하지 않았다.** #3 에서 규정 문서를 앞에 세워 이 함정을 *피했을 뿐 고친 게 아니라는 것*이
드러났기 때문이다. 근거에 들어오기만 하면 모델은 여전히 그걸 고른다. 근본을 #7 에서 고쳤다.

---

## #7 다른 제도를 다루는 쪽을 근거에서 뺀다

- **고친 곳**: `sources.yaml` 에 `쪽제외: ["해외에서 발송되는", "발송되는 술"]`, `fetch_docs.fetch_pdf` 가 그 쪽을 건너뛴다
- **왜**: 사례집 24쪽은 「우리나라 거주자가 해외에서 **발송되는** 술, 담배를 반입하는 경우」 —
  **해외직구** 기준이다. 이 봇은 여행자가 **직접 들고 오는** 경우만 다룬다.
  같은 "술 면세" 제목 아래 숫자가 완전히 다르고(150달러·1병 vs 2L·400달러), 근거에 있으면 모델이 인용한다.
- 지우는 것이 아니라 **범위를 맞추는 것**이다. 우리가 답하지 않는 제도의 문서는 애초에 근거가 아니다.

| 회차 | 도구 | 답변 | 실패 건 |
|---|---|---|---|
| imp7-1 | **100%** | **100%** | 없음 |
| imp7-2 | **100%** | **100%** | 없음 |
| imp7-3 | **100%** | **100%** | 없음 |

**판정: 수정됨.** 3회 연속 만점. 시연 문의도 바르게 답한다:

```
문의   예전엔 술 2병까지 됐잖아요. 지금은 어떻게 되나요?
답변   현재 주류 면세범위는 전체 용량이 2L 이하이고 총 가격이 미화 400달러 이하입니다.
       만 19세 미만(출생년도 기준) 미성년자는 주류 면세범위가 없습니다. (2026-09-17 기준)
```

과거 전제("예전엔 2병")를 **반박하지도, 따라가지도 않고** 현재 기준만 답한다.

---

## 분류가 애매한 질문으로 찔러 보기 (`probe_ambiguous.py`)

평가셋 12건은 카테고리가 깔끔하게 갈리는 문항만 있다. 실제 문의는 두 영역에 걸친다.
경계에 일부러 걸치는 8건을 만들어 돌렸다. **채점하지 않는다** — 어디로 가는지 보는 것이 목적이다.

**결과: 8건 중 3건 실패.** 12건이 100% 이던 시점이다.

| | 질문 | 걸치는 지점 | 결과 |
|---|---|---|---|
| 3 | 독일 소시지 30달러어치 샀는데 면세 한도 안이면 괜찮죠? | 면세 ↔ 검역 | ❌ **"신고 없이 통과할 수 있다"** |
| 4 | 캐비어를 선물로 받았는데 가져올 수 있나요? | 검역 ↔ 멸종위기종 | ❌ CITES 를 못 봄 |
| 6 | 해외 쇼핑몰에서 택배로 받는 술은 몇 병까지? | 면세 ↔ 범위밖 | ❌ 휴대품 기준을 적용 |
| 2 | 입국장면세점에서 산 위스키도 술 면세 한도에? | 면세점 ↔ 면세 | ⚠️ 공제 순서를 흐리게 설명 |
| 1·5·7·8 | 악어가죽+800달러 / 사향 화장품 / 반려견 / 폐기 | | ✅ |

3번이 위험했다. **검역이 막는 물건에 "신고 없이 통과"** 라고 답했다. 믿고 신고 안 하면 과태료다.

---

## #8 경로 규칙 — 부쳐서 받는 것은 범위밖

- **고친 곳**: `prompts.CLASSIFY` 한 줄 — *"택배·배송대행·해외직구처럼 물건만 부쳐서 받는 경우는
  면세 기준이 아예 다른 제도이므로 범위밖"*
- **왜**: #7 에서 직구 문서를 근거에서 뺐더니 **반대 방향 오류**가 생겼다 — 직구 질문에 휴대품 기준을 답한다.
  문서를 뺐으면 질문도 받지 말아야 한다.

| 회차 | 도구 | 답변 |
|---|---|---|
| imp8-1 | 100% | 100% |
| imp8-2 | 100% | 100% |

**판정: 수정됨 (6번).** 12건 회귀 없음.

---

## #9 금지·제한 품목 사전 확인 — 도구를 둘 부른다

- **고친 곳**: `context.RESTRICTED` + `assemble()` — 문의에 **물건 자체가 막히는 품목**(소시지·치즈·생과일 /
  악어·상아·산호…)이 있으면 분류가 무엇이든 그 영역의 도구를 **같이** 부른다
- **왜**: 분류는 *"무엇을 묻는가"* 를 맞혔다. 3번은 금액을 물었으니 `면세` 가 맞다.
  그런데 답에 필요한 것은 *"무엇을 가져오는가"* 였다. 한 문의에 도구 하나라는 제약이 문제였지
  분류가 틀린 게 아니다 — **프롬프트로는 고칠 수 없는 종류다.**
- 이미 그 영역을 보고 있으면 두 번 부르지 않고, **범위밖은 품목이 무엇이든 도구를 부르지 않는다**

| 회차 | 도구 | 답변 |
|---|---|---|
| imp9-1 | 100% | 100% |
| imp9-2 | 100% | 100% |

**판정: 수정됨 (3번).** 12건 회귀 없음.

```
독일 소시지 30달러어치 샀는데 면세 한도 안이면 괜찮죠?
  도구 ['lookup_duty', 'lookup_quarantine']
  → 800달러 이하에 해당하지만, 육류·육가공품은 수입 가능국가에서 수출국 검역증명서를
    휴대한 경우에만 검사 후 반입 가능합니다. 검역증명서가 없으면 반입이 제한될 수 있습니다.
```

**4번(캐비어)은 남았다.** 품목 사전에는 있지만 `docs/` 의 CITES 문서에 「철갑상어」가 없어
검색이 빈 결과를 돌려주고, 그래서 도구가 붙지 않는다. **문서 부족**이지 배선 문제가 아니다
(#1 의 악어가죽과 같은 종류). 봇은 "자료에 없다"고 정직하게 답하므로 위험하지는 않다.

---

## #10 CITES 종 목록 절을 받아오고, 용어 연결을 명시한다

**한 회차에 두 곳을 고쳤다.** 규칙을 어긴 것이 아니라, **반쪽만으로는 아무 효과가 없음이 증명되는**
짝이었다 — 목록을 받아와도 `캐비어` 가 목록의 `철갑상어` 와 안 이어지고, 연결만 만들어도 목록이 없다.

### ① 목록 문서 — 필요한 절만 떼어 온다

`철갑상어` 가 실려 있는 페이지는 **관세청 여행자 통관 안내 하나뿐이었다.** (다른 세관 페이지 4곳,
국립생물자원관·국립생태원·환경부까지 찔러 봤지만 목록이 짧거나 자바스크립트로 그려진다.)
그런데 그 페이지는 이미 `duty.md` 로 **면세** 에 쓰고 있다.

- **고친 곳**: `fetch_docs.fetch` 에 `절키워드` 추가 — 페이지를 통째로 복사하지 않고 **절 단위로** 가져온다
- `sources.yaml` 에 `cites_species.md` (같은 URL, `절키워드: ["철갑상어"]`, 카테고리 `멸종위기종`)
- 처음엔 `["CITES"]` 로 걸었더니 **가산세 절까지 딸려왔다** (그 절도 CITES 를 언급한다).
  품목 목록 절에만 있는 말로 좁혔다

### ② 용어 연결을 답변 단계에 넘긴다

목록을 넣고 도구도 둘 다 불렀는데 봇은 여전히 *"자료에 없다"* 고 답했다.

근거에 `철갑상어` 가 있어도 **«캐비어 = 철갑상어 알» 은 문서에 없는 연결**이라 모델이 스스로 잇지 않았다.
**옳은 태도다** — 그렇게 이어 주기 시작하면 근거 없는 추론이 열린다.

- **고친 곳**: `context.aliases()` + `prompts.ANSWER` 에 `[용어 연결]` 칸 —
  이 문의에 실제로 적용된 «사람 말 → 문서 말» 짝만 넘긴다
- *"이 표에 있는 것만 같은 것으로 본다"* 고 못 박았다. 모델이 연결을 늘리지 못한다
- **데모 화면에도 그대로 보인다** (③ 용어 연결). 우리가 만든 연결임을 보는 사람이 판단할 수 있게

| 회차 | 도구 | 답변 |
|---|---|---|
| imp10-1 | 100% | 100% |
| imp10-2 | 100% | 100% |

**판정: 수정됨 (4번).** 12건 회귀 없음.

```
캐비어를 선물로 받았는데 가져올 수 있나요?
  분류 검역 · 도구 ['lookup_quarantine', 'lookup_cites'] · 용어연결 캐비어 → 철갑상어
  → 캐비어(철갑상어)는 CITES 에서 보호하는 품목입니다. 살아있는 동물뿐 아니라
    이들을 사용하여 만든 제품·가공품도 포함됩니다. (…) 출국 전 재확인하시기 바랍니다.
```

---

## #11 별도 한도 품목도 교차 조회, 그리고 문서 용어 해설

경계 질문 2번 「입국장면세점에서 산 위스키도 술 면세 한도에 포함되나요?」 가 남아 있었다.

> (전) 입국장면세점에서 구매한 국산 술은 면세범위에서 우선 공제되어 면세됩니다. 따라서
> 입국장면세점에서 산 위스키가 **국산 술이라면 술 면세 한도에 포함되어** 면세 처리됩니다.

원인이 **세 겹**이었다. 한 겹씩 벗겼다.

### ① 교차 조회에 "별도 한도" 이유를 더한다

`면세점` 으로만 조회되어 **주류 면세범위(2L·400달러)가 근거에 없었다.**
#9 의 교차 조회는 「금지·제한 품목」만 다뤘는데, 술·담배·향수는 막히는 게 아니라
**기본 800달러와 별도 한도를 갖는** 품목이다. 이유가 다르지만 필요한 동작은 같다.

- `RESTRICTED` → `CROSS_CHECK` 로 이름을 바꾸고 `"면세": ("술","위스키","담배","향수"…)` 를 더했다
- 이름을 바꾼 이유: `RESTRICTED` 에 술이 들어가면 **"술은 금지 품목"** 으로 읽힌다.
  틀린 이름은 나중에 더 비싸다

→ 도구가 `['lookup_dutyfree_shop', 'lookup_duty']`, 2L·400달러가 근거에 들어왔다. **그래도 틀렸다.**

### ② 「우선 공제」를 잘못 읽는다

모델이 「면세범위에서 **우선 공제**」를 *"면세범위에서 빠진다"* 로 읽었다.
먼저 **차감**된다는 뜻이다 — 포함되는데 순서가 먼저인 것이다.

`prompts.ANSWER` 에 `[문서 용어]` 칸을 만들었다. 관공서 문서의 짧은 말은 뜻이 좁다.

```
「우선 공제」·「우선 차감」 = 면세범위에서 먼저 빼 준다. 면세범위 밖이라는 뜻이 아니다
「별도」 = 기본 면세범위와 따로 계산되는 자기 한도가 있다. 한도가 없다는 뜻이 아니다
「합산하여 과세」 = 어디서 샀든 전부 더해서 한도를 넘는지 본다
```

→ 아직 *"술 면세 한도에 포함되지 않고 별도 면세 대상"* 이라고 답했다.

### ③ 한도가 두 개인 것을 모른다

「기본 면세범위 800달러」와 「주류 면세범위 2L·400달러」가 **다른 한도**인데 하나로 뭉쳤다.
질문의 "술 면세 한도"는 후자를 가리킨다.

같은 `[문서 용어]` 칸에 한도 두 개를 갈라 적었다.

| 회차 | 도구 | 답변 |
|---|---|---|
| imp11-1 | 100% | 100% |
| imp11-2 | 100% | 100% |

**판정: 부분 수정.** 12건 회귀 없음. 위험한 오독(한도 밖이라고 답하는 것)은 사라졌고
핵심 사실이 들어왔다.

```
→ 입국장면세점에서 구매한 국산 술은 주류 면세범위에서 우선 면세처리됩니다. (…)
  국산 술이라면 주류 면세범위에 포함되어 면세되지만, 해외에서 구매한 위스키는
  주류 면세범위 내에서 합산하여 과세 여부를 판단합니다.
  주류 면세범위는 전체 용량 2L 이하이고 총 가격 400달러 이하입니다.
```

**국산/해외를 나눠 말하는 문장이 아직 어색하다. 여기서 멈췄다.**
근거에는 사실이 다 들어와 있고(합산 절·2L 절), 남은 것은 모델이 두 문장을 잇는 방식이다.
프롬프트 규칙을 더 쌓는 것이 **27회차의 함정**이었다. 다음에 손대려면 프롬프트가 아니라
`gpt-4.1` 같은 큰 모델로 이 문항만 다시 재 보는 쪽이 낫다.

---

## 애매한 문의로 찔러 보기 — 되묻기가 필요한 경우 (`probe_ambiguous.py 애매`)

경계 질문은 **분류가 걸치는** 어려움이었다. 이번은 다른 어려움이다 —
**사람이 애매하게 말해서 한 번 더 물어야** 하는 경우. 빠진 정보가 답을 뒤집는다.

8건을 만들어 돌렸다. **되묻기 0건. 8건 모두 그냥 답했다.**

| | 질문 | 처음 답변 |
|---|---|---|
| 4 | 가방 하나 샀어요. 신고해야 되나요? | ❌ 금액도 재질도 안 묻고 *"신고할 물품 없으면 제출 안 해도"* |
| 6 | 동물 가죽으로 만든 제품인데 문제 있을까요? | ❌ *"동물 가죽은 CITES 규제 부분품에 해당"* — 소가죽도 규제로 읽힌다 |
| 2 | 술 좀 사왔어요. | ⚠️ 기준만 나열. 되묻지 않음 |
| 5 | 한약재 가져와도 돼요? | ⚠️ 조건부 + "일반 약재는 자료에 없다" |
| 1 | 이거 세금 내야 하나요? | ○ 정보가 없다고 말하고 기준 안내 + "알려주시면" |
| 3·7·8 | 고기 / 친구 부탁 / 한도 초과 | ○ 조건부로 답함 |

파이프라인에 **되묻는 경로가 아예 없었다.** 답변과 넘김, 두 길만 있었다.

---

## #12 되묻기 경로 (gate → ask)

- **고친 곳**: `agent.gate` + `agent.ask` 노드, `prompts.GATE` · `prompts.ASK`
- **분류·넘기기와 따로 판단한다** (과제 요구: *"카테고리를 고르는 일과 확신이 없을 때 넘기는 판단을 분리"*).
  나가는 길이 셋이 되었고, 셋을 가르는 판단이 서로 다른 곳에 있다:

| 길 | 어디서 정하나 | 기준 |
|---|---|---|
| 답변 | — | 근거로 답할 수 있다 |
| **되묻기** | `gate` | **규정을 고를 수 없다** (품목이 특정되지 않아 적용 규정이 갈린다) |
| 넘김 | `classify`(범위밖) / `assemble`(검색 점수 0) | 근거가 없다 |

**모델의 확신이 기준인 곳은 한 곳도 없다.**

- `ask` 는 **도구를 하나도 부르지 않는다** (PLAYBOOK 3-③: 식별자 없으면 조회 금지).
  문구도 모델에게 다시 쓰게 하지 않고 템플릿으로 만든다 — 틀릴 여지만 늘고 비용도 는다

### 첫 시도: 게이트가 너무 넓었다 ⚠️ 큰 회귀

| 회차 | 도구 | 답변 | 실패 |
|---|---|---|---|
| imp12-1 | **8/12 = 66.7%** | 66.7% | Q1 Q2 Q3 C1 |
| imp12-2 | **9/12 = 75.0%** | 75.0% | Q2 Q3 C1 |

**100% → 66.7%.** 소시지·망고·김치처럼 **품목이 특정되어 조건부로 답할 수 있는 것**까지 되물었다.
`PLAYBOOK 4-⑤` 가 경고한 그대로다 — *"이관 게이트를 넓게 잡았더니 맞게 답하던 5건까지 사람에게 넘어갔다.
점수는 안 떨어져도 자동화율을 갉아먹으면 개선이 아니다."*

### 두 번째: 기준을 좁혔다 (프롬프트)

*"기준은 하나다 — **어느 규정을 적용할지 고를 수 있는가.**"*
품목이 특정되면 수량·금액·국가를 몰라도 **조건부로 답한다.** 「소시지」「망고」「김치」를 예시로 박았다.

| 회차 | 도구 | 답변 | 실패 |
|---|---|---|---|
| imp12b-1 | 11/12 = 91.7% | 91.7% | Q2 (망고) |
| imp12b-2 | 11/12 = 91.7% | 91.7% | Q2 (망고) |

**프롬프트에 「망고 → 모든 생과실은 수입금지」 라고 적어 놓았는데도 되물었다.**
네 번 고쳐도 안 됐다 → `PLAYBOOK 4-①`: **말로 안 고쳐지면 말이 문제가 아니다.**

여기서 키워드 규칙("품목이 사전에 있으면 되묻지 않는다")을 쌓을 수 있었지만 **하지 않았다.**
그건 부류가 아니라 케이스에 걸리는 코드 관문이고, 27회차가 그렇게 시작됐다.

### 세 번째: 게이트만 큰 모델로

- **고친 곳**: `GATE_MODEL = "gpt-4.1"` (분류·답변은 그대로 `gpt-4.1-mini`)
- 한 문의에 한 번 부르는 호출이라 비용 차이가 작다

| 회차 | 도구 | 답변 | 실패 |
|---|---|---|---|
| imp12c-1 | **12/12 = 100%** | **100%** | 없음 |
| imp12c-2 | **12/12 = 100%** | **100%** | 없음 |

**판정: 수정됨.** 12건 회귀 없이 되묻기 경로가 붙었다.

### 애매 8건 — 처음과 지금

| | 질문 | 처음 | 지금 |
|---|---|---|---|
| 1 | 이거 세금 내야 하나요? | 답변 | **되묻기** (품목·금액) |
| 2 | 술 좀 사왔어요. | 답변 | 답변 (2L·400달러 조건부) |
| 3 | 고기 좀 사왔는데 괜찮아요? | 답변 | **되묻기** |
| 4 | 가방 하나 샀어요. 신고해야 되나요? | ❌ 답변 | **되묻기** (재질·금액) |
| 5 | 한약재 가져와도 돼요? | 답변 | **되묻기** (어떤 약재) |
| 6 | 동물 가죽 제품인데 문제 있을까요? | ❌ 답변 | **되묻기** (어떤 동물) |
| 7 | 친구가 부탁해서 대신 사온 것도? | 답변 | 답변 (자료에 없다고 밝힘) |
| 8 | 면세 한도 넘었는데 어떻게 해요? | 답변 | 답변 (30% / 40% 둘 다) |

**되묻기 0건 → 5건.** 답하는 3건은 전부 조건부로 답이 되는 것들이다.

```
가방 하나 샀어요. 신고해야 되나요?
  분류 면세 · 도구 [] (되묻기)

  말씀만으로는 판단이 어렵습니다.
  정확히 안내해 드리려면 아래를 알려주세요.
  - 가방 종류(예: 일반 가죽, 악어가죽 등)
  - 구입 금액
```

---

## 제품명·브랜드로 묻는 경우 (`probe_ambiguous.py 제품명`)

사람은 품목 이름이 아니라 **제품명·브랜드**로 말한다. 「육포」 대신 「비첸향」, 「생햄」 대신 「하몽」.
우리 사전(`SYNONYMS`·`CROSS_CHECK`)은 손으로 적은 목록이라 브랜드는 하나도 모른다.

8건을 만들어 돌렸다. **4건 넘김.**

| | 질문 | 처음 |
|---|---|---|
| 1 | 홍콩에서 비첸향 사왔는데 괜찮을까요? | ❌ 넘김 |
| 2 | 스페인에서 하몽 한 덩어리 사왔어요. | ❌ 넘김 |
| 4 | 에르메스 크로커딜 백을 하나 샀는데 문제될까요? | ❌ 넘김 |
| 6 | 태국에서 두리안 사왔는데 되나요? | ❌ 넘김 — **대조군인데 실패** |
| 3·5·7·8 | 맥캘란 / 푸아그라 / 샥스핀 / 고디바 초콜릿 | 답변 |

**6번이 뜻밖이었다.** 「두리안」 은 `CROSS_CHECK` 에 적어 두었는데도 넘겼다 —
문서에 「두리안」 이라는 말이 없어 **검색 점수가 0** 이었다.
품목 사전에 적는 것과 문서 어휘로 이어 주는 것은 **다른 일**이다.

브랜드를 사전에 적어 넣는 것은 끝이 없다. 그게 케이스별 코드 관문이다.

---

## #13 문서 어휘로 정규화 + 근거 절 필터

### ① 닫힌 목록에서 고르게 한다

- **고친 곳**: `context.DOC_TERMS`(문서가 실제로 쓰는 말 34개) + `agent.normalize` 노드
- 모델에게 **이 목록 안에서만** 고르게 한다. 목록 밖은 못 고르니 **지어낼 여지가 없다**
- 목록의 말은 전부 `docs/` 에서 확인했다 — 문서에 없는 말을 넣으면 검색이 못 찾는다
  (`씨앗`·`자단`·`건강기능식품` 은 문서에 없어서 넣지 않았다)
- 고른 결과는 `[용어 연결]` 로 답변 단계에 넘기고 **데모 화면에도 보인다**

→ 하몽·크로커딜·두리안이 바르게 답했다. **비첸향만 「식물류에 해당」 이라고 답했다.**

### ② 충돌하는 절을 근거에서 뺀다

정규화는 `비첸향 → 육가공품` 을 맞게 골랐고 검색도 **육가공품 절을 1위로** 가져왔다.
그런데 답변 모델이 2위의 **식물 검역 절**을 골라 *"비첸향은 식물류"* 라고 답했다.

프롬프트로 네 번 막았다 — `[용어 연결]` 을 강하게 쓰고, 지켜야 할 것에 4번을 추가하고,
*"식물류 절이 같이 있어도 식물류로 답하지 않는다"* 까지 적었다. **계속 틀렸다.**

- **고친 곳**: `context.assemble` — 정규화가 품목을 특정했으면 **그 말이 들어 있는 절만** 근거로 쓴다
- 충돌하는 절을 **애초에 주지 않는 편**이 프롬프트보다 확실하다

| 회차 | 도구 | 답변 |
|---|---|---|
| imp13-1 | 100% | 100% |
| imp13-2 | 100% | 100% |

**판정: 수정됨.** 12건 회귀 없음. 브랜드 8건 중 넘김 4건 → 0건.

---

## #14 정규화를 항상 먼저, 그 결과를 교차 조회에 쓴다

#13 에서 정규화를 «근거를 못 찾았을 때만» 불렀다. 그랬더니 **하몽이 `면세` 로 분류된 경우**
면세 문서에서 근거가 찾아지긴 해서 정규화가 돌지 않고, **검역증명서 안내가 빠졌다.**

> 하몽 한 덩어리는 「동물(고기·가죽·털을 포함한다)」에 해당하므로, 반입 시 면세 범위 초과
> 물품에 해당할 수 있습니다. (…) 자세한 통관 절차나 허용 여부는 안내된 자료에 없습니다.

- **고친 곳**: `normalize` 를 `gate` 다음에 **항상** 돌리고, 그 결과를 `CROSS_CHECK` 교차 조회에도 쓴다
- `CROSS_CHECK` 에 문서 어휘(`육가공품`·`생과실`·`철갑상어`…)를 더했다 —
  손으로 적은 브랜드 목록 대신 **정규화 결과가 교차 조회로 들어온다.** 두 기능이 이어졌다
- 비용: 문의당 `gpt-4.1` 호출이 하나 늘었다(게이트 + 정규화 = 2회). 정직하게 적어 둔다

### 넣다가 만든 문제 두 개

**① 부분 일치로 과잉 발동.** `CROSS_CHECK["멸종위기종"]` 에 `가공품` 을 넣었더니
「육**가공품**」 에 걸려 검역 문의까지 CITES 를 불렀다. 종 이름으로만 걸도록 뺐다.

**② 제도 어휘로 절을 걸러 D3 가 깨졌다 (100 → 91.7%, 2회 연속).**
정규화가 `자진신고`·`가산세` 같은 **제도 어휘**까지 돌려주는데, #13 의 근거 필터가
그걸로 절을 걸러 **나란히 있던 「자진신고 30% 경감」 절이 빠졌다.**
→ `DOC_TERMS` 를 `ITEM_TERMS`(무엇을 가져오는가)와 `RULE_TERMS`(무엇을 묻는가)로 갈랐다.
**필터는 품목 어휘로만 건다.** 제도 어휘는 검색어로만 쓴다.

| 회차 | 도구 | 답변 | |
|---|---|---|---|
| imp14-1 | 100% | 91.7% | 제도 어휘까지 걸렀을 때 |
| imp14-2 | 100% | 91.7% | 같음 — D3 |
| imp14b-1 | **100%** | **100%** | 품목 어휘로만 |
| imp14b-2 | **100%** | **100%** | 같음 |

**판정: 수정됨.** 하몽이 `면세` 로 분류돼도 검역을 교차 조회한다.

### 제품명 8건 — 처음과 지금

| | 질문 | 처음 | 지금 |
|---|---|---|---|
| 1 | 비첸향 | 넘김 | 검역 → 육가공품·검역증명서 |
| 2 | 하몽 | 넘김 | 면세 + **검역 교차** → 육가공품 |
| 3 | 맥캘란 18년 두 병 | 답변 | 답변 (2L·400달러) |
| 4 | 에르메스 크로커딜 백 | 넘김 | 멸종위기종 → 악어·가죽 |
| 5 | 푸아그라 통조림 | 답변 | 면세 + 검역 교차 |
| 6 | 두리안 | 넘김 | 검역 → 생과실 수입금지 |
| 7 | 샥스핀 | 답변 | 멸종위기종 + 검역 교차 |
| 8 | 고디바 초콜릿 20박스 | 답변 | 답변 — **과잉 발동 없음** (대조군 통과) |

**넘김 4건 → 0건.** 대조군(초콜릿)이 검역으로 새지 않았다.

---

## #15 멀티턴 — 되묻고 나서 이어서 판정한다

되묻기(#12)를 붙였더니 **되묻고 끝났다.** 사용자가 답할 곳이 없었다.

- **고친 곳**: `agent.run(query, history)` · `history_block()` · `search_text()` ·
  `converse()`, 그리고 `CLASSIFY`·`GATE`·`ANSWER` 에 `[이전 대화]` 칸
- **핵심은 «따로 넘기기»다** (PLAYBOOK 4-④). 이전 대화를 이번 발화에 **이어 붙이지 않는다.**
  한 덩어리로 주면 봇이 **이전 질문에 답한다** — 모두몰 때 1턴 78% / 2턴 47% 였고
  그 차이의 큰 몫이 이것이었다

| 무엇에 | 이전 대화를 | 왜 |
|---|---|---|
| 분류·게이트·답변 | **따로** (`[이전 대화]` 칸, 배경이라고 명시) | 답할 대상은 이번 발화다 |
| 검색 | **합친다** (`search_text`) | 품목은 1턴에, 수량·재질은 2턴에 나온다 |

찾는 일과 답하는 일에 다른 문맥이 필요하다. 한 가지로 통일하면 한쪽이 깨진다.

- 게이트에 *"이전 대화에서 이미 물었고 사용자가 답했다면 다시 묻지 않는다.
  두 번 묻는 것이 되묻기보다 나쁘다"* 를 박았다
- 이전 대화는 **최근 3턴**만 넘긴다. 길면 배경이 주제를 덮는다

| 회차 | 도구 | 답변 |
|---|---|---|
| imp15-1 | **100%** | **100%** |
| imp15-2 | **100%** | **100%** |

**판정: 수정됨.** 12건(단일 턴) 회귀 없음.

### 대화 5건 (`probe_multiturn.py`)

| | 1턴 → 2턴 | 결과 |
|---|---|---|
| 1 | 가방 하나 샀어요 → **악어가죽 가방이고 500달러예요** | 되묻기 → 어휘 `악어·가죽` → CITES 허가 필요, *"가격과 관계없이"* ✅ |
| 2 | 이거 세금 내야 하나요? → **면세점 위스키 1L 두 병, 300달러** | 되묻기 → 어휘 `주류` → 2L·400달러 **한도 안** 판정 ✅ |
| 3 | 한약재 가져와도 돼요? → **웅담이요** | 되묻기 → 어휘 `웅담·한약` → CITES 증명서·밀수 처벌 ✅ |
| 4 | 동물 가죽 제품인데 → **그냥 소가죽이에요** | 되묻기 → *"소가죽은 CITES 대상에 해당하지 않습니다"* ✅ |
| 5 | 면세 한도가 얼마예요? → **그럼 술은요?** | 답변 → **되묻지 않고** 주류 한도로 이어받음 ✅ |

4번이 중요하다. 되묻기가 없었을 때는 *"동물 가죽은 CITES 규제 부분품에 해당"* 이라고
소가죽까지 규제로 답했다. 지금은 **대상이 아니라고 분명히** 말한다.

5번은 «되묻지 않아야 하는» 대조군이다. 「그럼 술은요?」 는 그것만 보면 품목이 두루뭉술하지만
이전 대화가 면세 한도였으므로 이어지는 질문이다. 게이트가 되묻지 않고 넘겼다.

### 데모도 대화형으로

`app.py` 를 `st.chat_message` / `st.chat_input` 으로 바꿨다. 답변마다 다섯 칸
(분류·도구·용어 연결·근거 절·검증)이 접이식으로 붙고, 마지막 턴만 펼쳐진다.
예시 버튼에 **💬**(대화가 이어짐)와 **↩**(넘김) 표시를 달았다.

---

## #16 답변을 세 토막으로 — 결론 · 준비물 · 자세히

지금까지 답변이 **한 덩어리 줄글**이었다. 사람들은 그걸 읽지 않는다.

- **고친 곳**: `agent.Reply` (구조화 출력) + `agent.render()` + `prompts.ANSWER` 의 답변 형식
- 마크다운으로 써 달라고 **부탁하지 않고 구조로 받았다** — 서식이 흔들리지 않고,
  데모 화면이 토막마다 다르게 보여 줄 수 있다

| 토막 | 무엇을 | 규칙 |
|---|---|---|
| **결론** | 한 문장. 되는지 안 되는지, 조건이 붙는지 | 여기에 설명을 넣지 않는다 |
| **준비물** | 입국할 때 **손에 들고 가야 하는 것**·**해야 하는 행동** | 기준·한도 같은 지식은 여기가 아니다 |
| **자세히** | 기준·한도·예외·처벌 | 3~6문장. 기준일은 여기 끝에만 |

「준비물」에 한도 지식이 섞이지 않도록 **손에 들고 가는 것이냐**로 선을 그었다.
검역증명서·허가서·신고서는 준비물, 「800달러 한도」는 자세히.

| 회차 | 도구 | 답변 | |
|---|---|---|---|
| imp16-1 | 100% | **100%** | |
| imp16-2 | 100% | 91.7% | C2 — 결론 압축 중 처벌 조항 누락 |
| imp16-3 | 100% | **100%** | |
| imp16-4 | 100% | **100%** | |

**판정: 수정됨.** 1건 차이는 변화 없음으로 센다(측정 규칙 4).
다만 **편차가 0이던 상태에서 ±1건이 생긴 것은 기록해 둔다** — 결론 한 문장으로 압축하면서
「관세법 제269조 밀수출입죄」 같은 조항이 빠질 때가 있다. 4회 중 1회.
다음에 이 문항이 또 흔들리면 결론과 자세히에 **처벌 조항은 반드시 자세히에 남긴다**를 넣는다.

```
스페인에서 하몽 한 덩어리 사왔어요.

**스페인에서 산 하몽은 수입 가능국가에서 수출국 검역증명서를 휴대한 경우에만 반입 가능합니다.**

**입국할 때 챙기세요**
- 수출국 검역증명서

휴대 축산물인 육가공품(햄 포함)은 수입 가능국가에서 수출국 검역증명서를 휴대한 경우에
한하여 검사 후 이상이 없으면 반입이 가능합니다. (…) (2026-09-17 기준)
```

되묻기·넘김은 토막이 없다 — 원래 짧다.

---

## 전체 흐름

| 회차 | 고친 곳 | 성격 | 도구 | 답변 |
|---|---|---|---|---|
| #0 기준선 | — | | 83.3% | 58.3% |
| #1 | 멸종위기종 문서 보강 | 문서 | 91.7% | 66.7% |
| #2 | 검색에 IDF | 검색 | 91.7% | 75.0% |
| #3 | 규정 문서 우선 (+ 회귀 1건 수정) | 검색 | 91.7% | 83.3% |
| #4 | 분류에 방향 규칙 | 프롬프트 | **100%** | 91.7% |
| #5 | 나란한 두 경우 둘 다 | 프롬프트 | **100%** | **100%** |
| #6 | 동의어 확장 | 검색 | 100% | 91.7% ⚠️ 회귀 |
| #7 | 다른 제도 쪽 제외 | 문서 | **100%** | **100%** |
| #8 | 경로 규칙 (택배는 범위밖) | 프롬프트 | **100%** | **100%** |
| #9 | 금지·제한 품목 교차 조회 | 구조 | **100%** | **100%** |
| #10 | CITES 종 목록 절 + 용어 연결 명시 | 문서 + 프롬프트 | **100%** | **100%** |
| #11 | 별도 한도 품목 교차 조회 + 문서 용어 해설 | 구조 + 프롬프트 | **100%** | **100%** |
| #12 | 되묻기 경로 (게이트만 큰 모델) | 구조 + 모델 | **100%** | **100%** |
| #13 | 문서 어휘 정규화 + 근거 절 필터 | 구조 | **100%** | **100%** |
| #14 | 정규화를 항상 먼저 + 품목/제도 어휘 분리 | 구조 | **100%** | **100%** |
| #15 | 멀티턴 (이전 대화를 따로 넘긴다) | 구조 | **100%** | **100%** |
| #16 | 답변을 세 토막으로 | 형식 | **100%** | **100%** (4회 중 1회 91.7%) |

**손댄 곳의 성격**: 문서 4 · 검색 3 · 프롬프트 5 · 구조 6 · 모델 1 · 형식 1.
케이스별 코드 관문은 **한 개도 넣지 않았다.**

## 12건 100% 가 말해 주지 않는 것

| 무엇이 찾아냈나 | 무엇을 | 회차 |
|---|---|---|
| 평가셋 12건 | 근거 선택, 사실 누락, 분류 방향 | #1~#5 |
| **시연 (사람이 직접)** | 한 글자 품목, 사람 말 ↔ 문서 말 | #6·#7 |
| **경계 질문 8건** | 한 문의 두 영역, 경로 구분, 문서에 없는 연결, 문서 용어 오독 | #8~#11 |
| **애매 질문 8건** | 되묻는 경로가 아예 없었다 | #12 |
| **제품명 질문 8건** | 브랜드·외래어를 문서 어휘로 이을 방법이 없었다 | #13·#14 |
| **대화 5건** | 되묻고 나서 이어지지 않았다 | #15 |
| **사람이 읽어 보고** | 답이 맞아도 **읽히지 않으면** 상담이 아니다 | #16 |

일곱 번 다 **12건이 만점인 상태에서** 나왔다. 100% 는 천장이 아니라 **평가셋의 크기**다.
그리고 #16 은 평가셋으로 **잴 수조차 없는** 문제였다 — 두 지표는 «사실이 담겼는가》만 본다.

찔러 보는 방법을 저장소에 남겼다 — `probe_ambiguous.py`(경계·애매·제품명 24건),
`probe_multiturn.py`(대화 5건). 채점하지 않고 **어디로 가는지** 본다.

## 다음에 할 일

1. **검증용 평가셋** — 29건을 정답과 함께 `data/holdout.json` 으로.
   되묻기 문항은 `expected_tools: []` 에 `되묻기: true` 를 더해 넘김과 구분한다
2. **2턴을 지표로 재기** — 지금 두 지표는 단일 턴만 본다. 모두몰 때 2턴이 1턴보다 31%p 낮았다
3. **결론 압축 편차** — C2 가 4회 중 1회 흔들렸다. 또 흔들리면 「처벌 조항은 자세히에 남긴다」 한 줄
4. **검역 문서 자동 갱신** — 주 1회 `fetch_docs.py 검역`

## 손대지 않기로 한 것

- `context.MAX = 600` (절 길이) — 지금 절 분할이 근거 표시 단위로 알맞다
- `verify` 를 숫자 외로 넓히지 않기 — 품목명까지 역추적하면 오탐이 폭발한다
- 평가셋을 프롬프트에 넣지 않기 (`data/fewshot.json` 만 예시로 쓴다)
- **`CROSS_CHECK`·`SYNONYMS` 에 브랜드를 적지 않기** — 정규화가 할 일이다. 적기 시작하면 끝이 없다
- **`DOC_TERMS` 에 문서에 없는 말을 넣지 않기** — 검색이 못 찾으므로 아무 효과가 없다
- **근거 필터를 제도 어휘로 걸지 않기** — 나란히 놓인 절이 빠진다 (D3 가 그렇게 깨졌다)
- **이전 대화를 이번 발화에 이어 붙이지 않기** — 봇이 이전 질문에 답한다
- **이전 대화를 3턴 넘게 넘기지 않기** — 배경이 주제를 덮는다
- **「준비물」에 기준·한도를 넣지 않기** — 손에 들고 가는 것과 알아야 하는 것은 다르다
- **용어 연결을 모델이 늘리게 하지 않기** — 목록에 있는 말만 고른다
- **`[문서 용어]` 를 더 늘리지 않기** — 한 문항을 위해 규칙을 쌓기 시작하면 27회차가 된다
- **되묻기 게이트를 넓히지 않기** — 조건부로라도 답이 되면 답하는 쪽. 한 번 넓혀서 100 → 66.7% 를 봤다
